import styled from "styled-components"

export const DivActions = styled.div(() => {
    return ({
        width: '100%',
        padding: '15px',
        display: 'flex',
        justifyContent: 'space-between',     
    })
})